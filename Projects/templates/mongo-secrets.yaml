apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-mongo-secrets-{{ .Release.Revision }}
data:
  MONGODB_USERNAME: {{ .Values.mongodb.auth.rootUser }}
  MONGODB_PASSWORD: {{ .Values.mongodb.auth.rootPassword }}
